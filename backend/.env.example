# Server Configuration
PORT=5001
NODE_ENV=development

# Meta Marketing / Graph API. Keep this on a supported version and upgrade it
# deliberately after reviewing Meta's changelog.
META_GRAPH_API_VERSION=v25.0

# Database Configuration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=market_ai_db
